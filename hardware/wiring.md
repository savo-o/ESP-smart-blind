# Подключение комплектующих

В этом разделе описано подключение основных компонентов проекта ESP Smart Blind.

## Используемые элементы

- ESP32
- Сервопривод 360° (continuous rotation)
- Провода Dupont (мама-мама / мама-папа)
- 3D-печатное колесо под шариковую цепочку

## Необязательные элементы

- Концевой выключатель (пока не используется)
- Понижающий преобразователь (при необходимости)
- Макетная плата (по желанию)

## Подключение сервопривода

Рассмотрим подключение на примере SG90 360° (continuous rotation).

Контакты сервопривода:

- 🔴 Красный - питание 5V  
- ⚫ Черный - GND (земля)  
- 🟡 Желтый - сигнальный провод (GPIO)

Важно:  
Сигнальный провод сервопривода должен быть подключен к GPIO 18.

Использование другого пина возможно, но в прошивке по умолчанию используется именно GPIO 18.

Важно: в схеме нарисован сервопривод SG90 360, но его не хватает на штору.
Схема подключения находится по пути: hardware/schemes/servo.png

## Подключение концевого выключателя

На данный момент не реализовано.  
Поддержка концевика будет добавлена в будущих версиях прошивки.

## Подключение понижающего преобразователя

На данный момент не реализовано.

Понижающий преобразователь потребуется в случае, если питание подается от источника выше 5V (например 12V).

## Подключение ИК-приемника

На данный момент не реализовано.

В будущих версиях планируется возможность управления шторой через ИК-пульт.

## Примечание


На первом этапе проект можно использовать без пайки, подключая все элементы через провода Dupont напрямую к ESP32.
